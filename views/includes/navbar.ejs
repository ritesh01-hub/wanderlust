<nav class="navbar navbar-expand-md border-bottom bg-body sticky-top">
  <div class="container-fluid px-4">

    <!-- Brand -->
    <a class="navbar-brand fw-semibold d-flex align-items-center gap-2" href="/">
      <i class="fa-regular fa-compass text-success fs-5"></i>
      Wanderlust
    </a>

    <!-- Toggler -->
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="mainNav">

      <!-- Search bar (centered) -->
      <form
        class="d-flex mx-auto my-2 my-md-0 search-form"
        action="/listings"
        method="GET"
        role="search"
      >
        <div class="input-group search-group">
          <input
            type="search"
            name="search"
            class="form-control border-end-0"
            placeholder="Search destinations..."
            aria-label="Search listings"
          >
          <button class="btn btn-outline-secondary border-start-0" type="submit">
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </div>
      </form>

      <!-- Right auth links -->
      <ul class="navbar-nav ms-auto mb-2 mb-md-0 align-items-center gap-2">
        <li class="nav-item">
          <a class="nav-link small" href="/listings/new">Airbnb your home</a>
        </li>
        <% if (currUser) { %>
          <li class="nav-item">
            <span class="nav-link text-muted small">Hi, <%= currUser.username %> 👋</span>
          </li>
          <li class="nav-item">
            <a class="btn btn-outline-secondary btn-sm" href="/logout">Log out</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="btn btn-outline-secondary btn-sm" href="/login">Log in</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-success btn-sm text-white" href="/signup">Sign up</a>
          </li>
        <% } %>
      </ul>

    </div>
  </div>
</nav>